// framework de nodejs
const express = require('express');
// nombre del servidor
const app = express();

// Settins
app.set('port',  proces.env.PORT || 3000);

// Middlewares

// Glbal Variables 

// Routes 

// Static Files

// Server is listening
app.listen(app.get('port'));
